{
  "name": "referendum-ja",
  "version": "0.0.1",
  "private": true,
  "description": "Verifiable petition platform for Andorra",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "build": "pnpm -r build",
    "audit:deps": "pnpm audit --audit-level=moderate"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "vitest": "^2.1.0"
  }
}
